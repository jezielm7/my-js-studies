//  Spread Operator

const values = ['A', 'B', 'C'];
const newValues = [...values, 'D'];

console.log(newNames);

// pop(): Remove um item no final do array;
// push(): Adiciona um item no final do array;

// shift(): Remove um item no início do array;
// unshift(): Adiciona um item no início do array;


// Remover um item específico do array:

// let list = ["bar", "baz", "foo", "qux"];
// list.splice(list.indexOf('foo'), 1);


// slice(): Particiona o array;

// const values = ['a', 'b', 'c', 'd', 'e'];
// const newValues = values.slice(1, -1);
// output: ['b', 'c', 'd'];


// split(): Transforma uma string em array;

// const name = 'Jeziel Marques Braga';
// const names = name.split(' '); // dentro se passa o param de separação.
// output: ['Jeziel', 'Marques', 'Braga'];

// join(): Transforma um array em string;

// const name = ['Jeziel', 'Marques', 'Braga'];
// const names = name.join(' '); // dentro se passa o param de junção.
// output: 'Jeziel Marques Braga';